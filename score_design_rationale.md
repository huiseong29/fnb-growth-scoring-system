# GroMong Score Design Rationale

이 문서는 GroMong Score의 5개 지수가 어떤 데이터, EDA 결과, 모델링 결과, 도메인 논리를 바탕으로 설계되었는지 정리한다.

본 프로젝트의 주제는 **AI 기반 F&B 성장 유망 매장 스코어링 시스템**이다. 핵심 차별점은 단순히 현재 주문수나 리뷰수가 높은 매장을 찾는 것이 아니라, **브랜드·카테고리·상권 효과를 보정한 초과 성장 잠재력**을 점수화한다는 점이다.

## 1. 스코어 설계의 기본 입장

현재 GroMong Score는 완전히 최적화된 최종 투자 모델이 아니라, 제공된 실제 F&B 운영 데이터를 기반으로 만든 **파일럿 스코어링 체계**이다.

즉, 다음을 목표로 한다.

```text
제공 데이터 안에서 성장 유망 매장을 설명 가능하게 선별하는 0~100점 점수 체계를 만든다.
```

현재 스코어는 다음에 기반한다.

- 제공 데이터 구조
- EDA 결과
- Baseline vs Growth Alpha 모델 결과
- 서울 외부 상권 데이터 효과 검증 결과
- F&B 운영/투자 관점의 도메인 논리

다만 현재 가중치는 통계적으로 완전히 최적화된 값은 아니다. 향후 실제 투자 성과 라벨이나 기업 제공 성장/하락 정답지가 확보되면 가중치 최적화와 calibration이 필요하다.

## 2. 최종 GroMong Score 공식

현재 기본 공식은 다음과 같다.

```text
GroMong Score =
0.35 * 성장 확률 점수
+ 0.25 * Growth Alpha 점수
+ 0.15 * 리뷰 성장 지수
+ 0.15 * 운영역량 지수
+ 0.10 * 안정성 지수
```

서울 외부 상권 데이터가 결합된 매장은 다음과 같이 외부 상권 적합도를 10% 보정한다.

```text
서울 매장 최종 점수 =
0.90 * 기본 GroMong Score
+ 0.10 * 상권 적합도 점수
```

## 3. 5개 지수 요약

| 지수 | 가중치 | 사용 변수 | 근거 성격 |
| --- | ---: | --- | --- |
| 성장 확률 점수 | 35% | 모델 예측 성장 확률 | 데이터/모델 기반 |
| Growth Alpha 점수 | 25% | `historical_internal_growth_alpha` | 프로젝트 차별점, 데이터 기반 |
| 리뷰 성장 지수 | 15% | `historical_review_growth_3m` | EDA 기반 |
| 운영역량 지수 | 15% | `reply_rate`, `avg_reply_delay_hours` | EDA + 도메인 기반 |
| 안정성 지수 | 10% | 최근 주문/리뷰 변동성 | 도메인 기반 |
| 상권 적합도 | 서울 매장 10% 보정 | `store_vs_market_ticket_ratio` | 외부 데이터/모델 기반 |

## 4. 지수별 설계 근거

## 4.1 성장 확률 점수

### 정의

성장 확률 점수는 Growth Alpha 모델이 예측한 성장 라벨 1 확률을 0~100점으로 변환한 값이다.

```text
성장 확률 점수 = 모델 예측 성장 확률 * 100
```

### 사용 데이터

- 주문수
- 매출
- 객단가
- 리뷰수
- 평점
- 답글률
- 응답속도
- 메뉴 수
- 메뉴 가격대
- 브랜드
- 카테고리
- 지역
- 실험군/통제군
- 과거 기반 Growth Alpha 변수

### 성장 라벨 정의

성장 라벨은 미래 3개월 성과를 기준으로 직접 생성했다.

```text
composite_growth_score =
0.5 * 주문 성장률
+ 0.3 * 매출 성장률
+ 0.2 * 리뷰 성장률
```

이 점수가 전체 유효 행 중 상위 30%에 해당하면 `growth_label_top30 = 1`로 정의했다.

### EDA 및 모델 근거

성장 라벨 1 그룹은 성장 라벨 0 그룹보다 주문수와 리뷰수가 높았다.

- 성장 라벨 1 평균 월 주문수: 169.17
- 성장 라벨 0 평균 월 주문수: 68.18
- 성장 라벨 1 평균 월 리뷰수: 14.90
- 성장 라벨 0 평균 월 리뷰수: 8.81

모델링 결과:

| 모델 | AUC | F1 |
| --- | ---: | ---: |
| Baseline | 0.6060 | 0.5173 |
| Growth Alpha | 0.6078 | 0.5169 |

### 설계 논리

성장 확률 점수는 가장 직접적인 AI 모델 출력이므로 전체 점수에서 가장 큰 비중인 35%를 부여했다.

### 한계

- 현재 모델은 로지스틱 회귀 기반 파일럿 모델이다.
- XGBoost, LightGBM, SHAP 기반 모델 고도화는 아직 수행하지 않았다.
- 실제 투자 성공 라벨이 아니라 주문/매출/리뷰 기반 파생 라벨을 사용했다.

## 4.2 Growth Alpha 점수

### 정의

Growth Alpha는 같은 브랜드와 같은 카테고리 평균을 보정한 초과 성장 신호다.

예측 모델과 최종 점수에는 미래 정보 누수를 피하기 위해 과거 데이터만으로 계산한 `historical_internal_growth_alpha`를 사용한다.

개념:

```text
Growth Alpha =
매장 과거 성장률
- 브랜드 평균 과거 성장률
- 카테고리 평균 과거 성장률
```

실제 구현에서는 브랜드 보정 성장률과 카테고리 보정 성장률의 평균을 사용했다.

### 사용 데이터

- 과거 3개월 주문 성장률
- 과거 3개월 매출 성장률
- 과거 3개월 리뷰 성장률
- 브랜드
- 카테고리

### EDA 및 모델 근거

결과 설명용 `internal_growth_alpha`는 성장 라벨 1과 0을 뚜렷하게 구분했다.

- 성장 라벨 1 평균 `internal_growth_alpha`: 2.9508
- 성장 라벨 0 평균 `internal_growth_alpha`: -1.2651

예측 모델에서는 과거 기반 `historical_internal_growth_alpha`를 사용했다.

Growth Alpha 모델은 Baseline보다 AUC가 소폭 개선되었다.

- Baseline AUC: 0.6060
- Growth Alpha AUC: 0.6078

Top N 추천에서도 개선이 있었다.

- Baseline Top 100 Precision: 0.400
- Growth Alpha Top 100 Precision: 0.420
- Baseline Top 200 Precision: 0.395
- Growth Alpha Top 200 Precision: 0.405

### 설계 논리

Growth Alpha는 본 프로젝트의 핵심 차별점이다.

단순히 현재 잘되는 매장을 찾는 것이 아니라, 같은 브랜드·카테고리 조건에서 초과 성장하는 매장을 찾기 위해 사용한다.

따라서 성장 확률 다음으로 높은 25% 가중치를 부여했다.

### 한계

- 과거 기반 Growth Alpha만으로는 강한 단독 예측력을 보이지 않았다.
- 현재는 예측 성능 개선보다 설명 가능성과 스코어 차별점 측면에서 더 큰 의미가 있다.
- 향후 브랜드별 calibration 또는 카테고리별 ranking 보정이 필요하다.

## 4.3 리뷰 성장 지수

### 정의

리뷰 성장 지수는 과거 3개월 리뷰 증가 흐름을 0~100점으로 변환한 지표다.

사용 변수:

- `historical_review_growth_3m`

### 사용 데이터

- 리뷰 생성일
- 월별 리뷰 수

### EDA 근거

성장 라벨 1 그룹은 성장 라벨 0 그룹보다 평균 리뷰 수가 높았다.

- 성장 라벨 1 평균 월 리뷰수: 14.90
- 성장 라벨 0 평균 월 리뷰수: 8.81

### 설계 논리

리뷰 증가는 고객 반응, 플랫폼 노출, 매장 활동성의 신호로 볼 수 있다.

F&B 매장에서 리뷰는 주문과 함께 중요한 성과 변수이므로, 최종 점수에 15% 반영했다.

### 한계

- 현재는 리뷰 텍스트 내용 자체를 활용하지 않았다.
- 리뷰 감성 분석, 키워드 분석, 부정 리뷰 유형 분석은 향후 고도화 과제다.

## 4.4 운영역량 지수

### 정의

운영역량 지수는 리뷰 응답 활동을 기반으로 매장의 고객 대응 역량을 측정한다.

사용 변수:

- `reply_rate`
- `avg_reply_delay_hours`

점수화 방향:

```text
답글률이 높을수록 좋음
응답 지연시간이 짧을수록 좋음
```

### 사용 데이터

- 리뷰 생성일
- 답글 생성일
- 답글 여부

### EDA 근거

성장 라벨 1 그룹은 성장 라벨 0 그룹보다 답글률이 높았다.

- 성장 라벨 1 평균 답글률: 0.5505
- 성장 라벨 0 평균 답글률: 0.4702

평균 응답 지연시간도 성장 라벨 1 그룹이 더 짧았다.

- 성장 라벨 1 평균 응답 지연시간: 2,791.93시간
- 성장 라벨 0 평균 응답 지연시간: 4,172.90시간

### 설계 논리

리뷰에 꾸준히 응답하는 매장은 고객 대응 활동이 활발하고, 운영 관리가 잘 되고 있을 가능성이 있다.

PDF에서 말한 운영역량지수(OCI)를 현재 제공 데이터에서 직접 만들 수 있는 proxy로 구현한 것이다.

### 한계

- 답글이 사장님 직접 댓글인지, AI 댓글인지, 마케팅 댓글인지 완전히 구분되지 않는다.
- 따라서 현재 운영역량 지수는 댓글 유형별 효과 분석이 아니라 응답 활동 기반 proxy이다.

## 4.5 안정성 지수

### 정의

안정성 지수는 최근 주문수와 리뷰수의 변동성이 낮을수록 높은 점수를 주는 지표다.

사용 변수:

- 최근 주문수 표준편차
- 최근 리뷰수 표준편차

### 사용 데이터

- 최근 월별 주문수
- 최근 월별 리뷰수

### 설계 논리

투자 관점에서는 단기적으로 급등락이 큰 매장보다, 안정적으로 성과를 유지하는 매장이 리스크가 낮을 수 있다.

PDF의 안정성지수(SI)를 현재 데이터에서 구현 가능한 방식으로 변환한 것이다.

### EDA 근거 수준

안정성 지수는 EDA에서 강하게 검증된 지표라기보다는 F&B 투자/운영 관점의 도메인 논리가 더 강하다.

### 한계

- 안정성이 높다고 반드시 성장성이 높은 것은 아니다.
- 고성장 초기 매장은 변동성이 높을 수 있으므로, 안정성 지수는 성장 지표를 보완하는 리스크 지표로 해석해야 한다.

## 5. 서울 상권 적합도

### 정의

서울 상권 적합도는 서울 매장 211개에 대해 외부 상권 평균 객단가 대비 매장 객단가 위치를 점수화한 값이다.

사용 변수:

- `store_vs_market_ticket_ratio`

개념:

```text
store_vs_market_ticket_ratio =
매장 평균 객단가 / 같은 자치구·유사업종 상권 평균 객단가
```

### 사용 외부 데이터

- 서울시 상권분석서비스 2024년 행정동 추정매출 데이터
- 자치구·유사업종 기준 추정매출
- 자치구·유사업종 기준 추정매출건수
- 자치구·유사업종 기준 평균 객단가

### 모델 근거

서울 매장 211개 subset에서 외부 상권 변수를 추가했을 때 성능이 개선되었다.

| 모델 | AUC | Top 100 Precision |
| --- | ---: | ---: |
| Seoul Internal | 0.5826 | 0.340 |
| Seoul External | 0.5995 | 0.370 |

또한 외부 변수 중 다음 변수가 중요 변수 상위에 포함되었다.

- `store_vs_market_ticket_ratio`
- `market_q4_avg_ticket`

### 설계 논리

상권 데이터는 본 프로젝트의 차별점인 **상권 대비 성장 유망성 평가**를 구현하기 위한 핵심 보조 지표다.

서울 매장에 한해서 최종 점수의 10% 보정 항목으로 반영했다.

### 한계

- 현재 외부 데이터는 서울 매장 211개에만 안정적으로 결합되었다.
- 결합 단위가 자치구라서 행정동/좌표 단위보다 정밀도가 낮다.
- 전국 확장을 위해서는 상가업소정보, 인허가/폐업 데이터, 좌표 변환이 필요하다.

## 6. 가중치 설정 이유

현재 가중치는 다음 원칙으로 설정했다.

| 항목 | 가중치 | 설정 이유 |
| --- | ---: | --- |
| 성장 확률 점수 | 35% | AI 모델의 직접 예측 결과이므로 가장 높은 비중 |
| Growth Alpha 점수 | 25% | 프로젝트 차별점이며 브랜드·카테고리 보정 성장성을 반영 |
| 리뷰 성장 지수 | 15% | EDA에서 성장 그룹의 리뷰 수가 높게 나타남 |
| 운영역량 지수 | 15% | EDA에서 성장 그룹의 답글률이 높고 응답속도가 짧게 나타남 |
| 안정성 지수 | 10% | 투자 리스크 관점에서 성과 변동성을 보완 |

서울 매장 상권 적합도는 별도 보정으로 10% 반영했다.

가중치 설정은 다음 성격을 가진다.

```text
데이터와 EDA, 모델 결과, 도메인 논리를 반영한 파일럿 설계값
```

현재 가중치는 다음을 수행한 결과는 아니다.

- grid search 기반 최적화
- AUC 최대화 기준 최적화
- 실제 투자 수익률 기반 calibration
- SHAP 기반 자동 가중치 산정

## 7. 발표에서 사용할 안전한 표현

추천 표현:

```text
본 프로젝트의 5개 지수는 제공 데이터 구조, EDA 결과, 모델 검증 결과, F&B 투자 도메인 논리를 바탕으로 설계한 파일럿 스코어링 체계입니다.
```

```text
현재 가중치는 최종 최적값이라기보다, 모델 예측 결과와 Growth Alpha 차별점을 중심으로 구성한 초기 설계값입니다.
```

```text
향후 실제 투자 성과 라벨이나 기업 정답지가 확보되면 가중치 최적화와 calibration을 통해 스코어를 고도화할 수 있습니다.
```

피해야 할 표현:

```text
이 가중치는 완전히 최적화된 값입니다.
```

```text
이 점수는 실제 투자 성공을 보장합니다.
```

```text
전체 F&B 시장에 바로 일반화할 수 있습니다.
```

## 8. 향후 고도화 방향

### 8.1 가중치 민감도 분석

다음 시나리오를 비교할 수 있다.

- 현재 가중치
- Growth Alpha 강화 가중치
- 모델 성장 확률 강화 가중치
- 운영역량 강화 가중치
- 안정성 강화 가중치

비교 지표:

- AUC
- F1
- Top N Precision
- 등급별 매장 분포

### 8.2 리뷰 텍스트 NLP 추가

현재 리뷰 원문은 사용하지 않았다. 향후 다음 분석을 추가할 수 있다.

- 리뷰 감성 분석
- 부정 리뷰 키워드 추출
- 별점과 감성 불일치 탐지
- F&B 특화 리뷰 주제 분류

### 8.3 처치 효과 분석

현재 `is_post_treatment`는 모델 feature로 사용했지만, 별도의 인과분석은 수행하지 않았다.

향후 가능 분석:

- 처치 전후 비교
- DID
- 이벤트 스터디
- 실험군/통제군별 성장률 차이

### 8.4 전국 외부 데이터 확장

현재 외부 상권 데이터는 서울 211개 매장에 안정 결합되었다.

향후 추가 후보:

- 소상공인시장진흥공단 상가업소정보
- 인허가/폐업 데이터
- 관광/방문자 데이터
- 주소 좌표 변환

## 9. 결론

현재 GroMong Score는 데이터를 전혀 보지 않고 임의로 만든 점수가 아니다.

제공 데이터 구조, EDA 결과, 모델 검증, 외부 상권 데이터 효과, F&B 도메인 논리를 종합해 만든 파일럿 스코어링 체계다.

다만 완전히 최적화된 최종 투자 모델은 아니며, 실제 투자 성과 라벨과 더 정교한 외부 데이터가 확보되면 고도화가 필요하다.
## 2026-05-08 보강 반영: 리뷰 원문 NLP와 스코어 고도화 방향

리뷰 원문 기반 NLP 파일럿 분석을 완료했다.

이번 분석은 현재 GroMong Score의 5개 지수 가중치를 즉시 변경하기 위한 작업이라기보다, 향후 **리뷰 품질 지수**를 고도화할 수 있는 근거를 마련한 작업이다.

확인된 신호:

- 성장 상위 매장은 평균 텍스트 감성이 더 높다.
- 성장 상위 매장은 긍정 리뷰 비율이 더 높다.
- 성장 상위 매장은 부정 리뷰 비율이 더 낮다.
- 성장 상위 매장은 재주문 언급률이 더 높다.

현재 스코어와의 연결:

```text
현재 GroMong Score는 리뷰 성장률을 15% 반영한다. 이번 NLP 분석 결과는 향후 이 리뷰 지수를 단순 리뷰 수 증가가 아니라, 리뷰 원문의 긍정/부정 신호와 재주문 의도까지 포함하는 방향으로 고도화할 수 있음을 보여준다.
```

발표에서 사용할 안전한 표현:

```text
이번 중간 단계에서는 리뷰 원문 NLP를 별도 보강 분석으로 제시하고, 최종 단계에서는 리뷰 성장 지수에 텍스트 감성 및 재주문 신호를 결합하는 방향으로 확장할 수 있습니다.
```

다음 검증:

- 현재 5개 지수 가중치가 바뀌어도 상위 추천 매장이 얼마나 안정적으로 유지되는지 가중치 민감도 분석을 수행한다.
## 2026-05-08 보강 반영: 가중치 민감도 분석 결과

GroMong Score의 5개 지수 가중치가 임의적이라는 지적을 방어하기 위해 가중치 민감도 분석을 수행했다.

비교한 시나리오:

- 현재 공식 가중치
- 성장확률 강화
- Growth Alpha 강화
- 리뷰 성장 강화
- 운영 역량 강화
- 안정성 강화
- 서울 상권 적합도 강화

핵심 결과:

| 시나리오 | Top100 겹침률 | 순위 상관 |
| --- | ---: | ---: |
| 성장확률 강화 | 91% | 0.990 |
| Growth Alpha 강화 | 89% | 0.964 |
| 리뷰 성장 강화 | 78% | 0.976 |
| 운영 역량 강화 | 94% | 0.983 |
| 안정성 강화 | 82% | 0.959 |
| 서울 상권 적합도 강화 | 95% | 0.995 |

해석:

```text
가중치가 바뀌어도 현재 Top100 후보의 78~95%가 유지되고, 전체 순위 상관도 0.959 이상으로 높게 나타났다. 따라서 현재 점수 기준은 하나의 임의 가중치에만 과도하게 의존한다고 보기 어렵다.
```

점수 설계에 대한 결론:

- 현재 가중치는 최종 정답값이 아니라 초기 설계값이다.
- 다만 민감도 분석 결과, 상위 후보군은 대체 가중치에서도 상당 부분 유지된다.
- 리뷰 성장 강화와 안정성 강화 시나리오에서 후보군 변화가 상대적으로 크므로, 최종 단계에서는 이 두 지표의 의미를 더 세밀하게 설명해야 한다.
- 최상위권이 특정 브랜드/카테고리에 집중되는 한계가 있어, 다음 단계에서 브랜드/카테고리 보정 점수를 추가한다.
## 2026-05-08 보강 반영: 브랜드/카테고리 보정 점수

제공 데이터가 특정 브랜드와 카테고리에 치우친다는 한계를 보완하기 위해 보정 점수를 추가했다.

보정 공식:

```text
calibrated_score =
0.50 * GroMong Score
+ 0.25 * 브랜드 내부 percentile
+ 0.25 * 카테고리 내부 percentile
```

설계 이유:

- 기존 GroMong Score를 50% 반영해 원래 모델/EDA/상권/운영 신호를 유지한다.
- 브랜드 내부 percentile을 25% 반영해 같은 브랜드 안에서 상대적으로 잘한 매장을 드러낸다.
- 카테고리 내부 percentile을 25% 반영해 같은 업종 안에서 상대적으로 잘한 매장을 드러낸다.

핵심 결과:

| 항목 | 보정 전 | 보정 후 |
| --- | ---: | ---: |
| 백반·죽·국수 Top100 포함 | 92개 | 84개 |
| 치킨 Top100 포함 | 7개 | 11개 |
| 피자 Top100 포함 | 1개 | 5개 |
| 굽네치킨 Top100 포함 | 8개 | 16개 |

해석:

```text
기존 Top100 중 92개는 보정 후에도 유지되어 기존 점수 체계의 안정성은 유지됐다. 동시에 치킨, 피자, 굽네치킨 후보가 더 많이 드러나 특정 브랜드/카테고리 편중을 일부 완화했다.
```

발표에서 사용할 안전한 표현:

```text
보정 점수는 기존 GroMong Score를 대체하는 최종 정답이 아니라, 제한된 데이터 안에서 브랜드와 카테고리 편중을 보완하기 위한 추가 관점입니다.
```

주의할 표현:

```text
보정 점수가 실제 투자 성공을 보장한다.
```

```text
보정 점수만 보면 더 정확하다.
```

위 표현은 사용하지 않는다. 대신 전체 점수, 보정 점수, 외부 상권, 리뷰 NLP를 함께 보는 후보 선별 시스템이라고 설명한다.
## 2026-05-08 진행 예정: 불균형 진단을 통한 스코어 방어

현재 GroMong Score는 전체 성장 가능성 기준의 점수이며, 데이터 불균형이 존재하는 상황에서는 특정 브랜드/카테고리 상위권 집중이 발생할 수 있다.

이를 방어하기 위해 다음 분석을 추가한다.

- 원본 데이터의 브랜드/카테고리 분포
- 기존 Top100의 브랜드/카테고리 분포
- 보정 Top100의 브랜드/카테고리 분포
- 전체 추천과 균형형 추천의 역할 구분

스코어 해석 원칙:

```text
GroMong Score는 전체 성장 가능성 후보를 찾는 기준이고, 균형형 추천은 제한된 데이터 안에서 브랜드/카테고리 편중을 완화해 후보군을 다양화하는 보조 기준이다.
```
## 2026-05-08 보강 반영: 불균형 진단과 2트랙 추천

불균형 진단 결과, 기존 Top100은 특정 카테고리에 강하게 집중되는 문제가 있었다.

핵심 수치:

| 구분 | 최대 카테고리 비중 |
| --- | ---: |
| 원본 전체 데이터 | 64.5% |
| 기존 Top100 | 92.0% |
| 보정 Top100 | 84.0% |
| 균형형 추천 | 42.9% |

이 결과를 바탕으로 스코어 해석을 다음처럼 정리한다.

```text
GroMong Score는 전체 성장 가능성 후보를 찾는 기준이고, 보정 점수는 브랜드/카테고리 내부 상대 우수성을 함께 반영한 기준이다. 균형형 추천은 발표와 의사결정에서 편중을 완화하기 위한 보조 후보군이다.
```

최종 발표에서 사용할 2트랙 구조:

1. 전체 성장 가능성 Top 후보
2. 브랜드/카테고리 균형형 후보

주의할 표현:

```text
불균형을 완전히 해결했다.
```

위 표현은 사용하지 않는다. 대신 다음처럼 말한다.

```text
불균형을 완전히 제거한 것은 아니지만, 원본-기존 Top100-보정 Top100-균형형 추천을 비교해 한계를 직접 진단하고 보완 방향을 제시했습니다.
```
